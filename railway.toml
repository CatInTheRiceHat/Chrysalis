[build]
builder = "nixpacks"
buildCommand = "cd website && npm install && npm run build"

[deploy]
startCommand = "uvicorn api:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/api/migration/today"
