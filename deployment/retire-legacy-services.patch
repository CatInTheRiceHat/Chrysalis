--- a/vercel.json
+++ b/vercel.json
@@ -4,10 +4,6 @@
   "buildCommand": "cd public-site && npm run build",
   "outputDirectory": "public-site/dist",
   "rewrites": [
-    {
-      "source": "/api/:path*",
-      "destination": "/api/index.py"
-    },
     {
       "source": "/",
       "destination": "/index.html"
@@ -49,16 +45,6 @@
       "destination": "/legacy/index.html"
     }
   ],
-  "crons": [
-    {
-      "path": "/api/cron/drop?mode=morning",
-      "schedule": "0 7 * * *"
-    },
-    {
-      "path": "/api/cron/drop?mode=evening",
-      "schedule": "0 19 * * *"
-    }
-  ],
   "headers": [
     {
       "source": "/(.*)",
--- a/.vercelignore
+++ b/.vercelignore
@@ -20,3 +20,15 @@
 .env*
 **/.env*
 supabase/.temp
+
+# Apply only after the separate legacy-service retirement decision.
+api
+api.py
+research_api.py
+core
+integrations
+migrations
+supabase
+supabase_schema.sql
+requirements.txt
+datasets
--- a/.github/workflows/youtube-feed-ingest.yml
+++ /dev/null
@@ -1,30 +0,0 @@
-name: YouTube Feed Ingest
-
-on:
-  schedule:
-    # 4x/day, evenly spaced every 6h, offset 7 min past the hour to dodge
-    # top-of-hour traffic spikes. GitHub Actions cron is always UTC.
-    # Runs at 00:07, 06:07, 12:07, 18:07 UTC.
-    - cron: "7 0,6,12,18 * * *"
-  workflow_dispatch:
-
-jobs:
-  ingest:
-    runs-on: ubuntu-latest
-
-    env:
-      CHRYSALIS_API_BASE_URL: ${{ secrets.CHRYSALIS_API_BASE_URL }}
-      FEED_INGEST_SECRET: ${{ secrets.FEED_INGEST_SECRET }}
-
-    steps:
-      - name: Validate required secrets
-        run: |
-          test -n "$CHRYSALIS_API_BASE_URL"
-          test -n "$FEED_INGEST_SECRET"
-
-      - name: Call YouTube ingest endpoint
-        run: |
-          curl -X POST "$CHRYSALIS_API_BASE_URL/api/admin/ingest/youtube" \
-            -H "X-Feed-Ingest-Secret: $FEED_INGEST_SECRET" \
-            --fail \
-            --show-error
\ No newline at end of file
