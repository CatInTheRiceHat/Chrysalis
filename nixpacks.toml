[phases.setup]
nixPkgs = ["python313", "nodejs_20"]

[phases.install]
cmds = ["python -m pip install -r requirements.txt"]

[phases.build]
cmds = ["cd website && npm install --legacy-peer-deps && npm run build"]

[start]
cmd = "uvicorn api:app --host 0.0.0.0 --port $PORT"
